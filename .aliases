#!/bin/bash

alias ll="ls -lahG"
alias cat="bat" # must 'brew install bat' first
alias chmox="chmod -x"
alias notes="$EDITOR ~/Documents/notes"
alias g="git"
alias gl="git log"
alias rspec-no-system='bundle exec rspec --exclude-pattern "spec/system/**/*"'